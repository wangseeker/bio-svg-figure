---
name: bio-svg-figure
description: 设计与生成高质量科学/教材示意图（SVG 矢量、NR 风）。融合 SVG 技术规范与设计出版规范（示意图类型/配色/箭头语义/≥7pt/色盲/出版合规），内置教材固定规范（150×100mm、三档字号、NR 色板、RDKit 分子、300dpi）。用于画通路/机制/分类/知识地图等教学与论文配图。Trigger on：画这类示意 SVG 时。
license: "CC-BY-4.0"
---

# Bio SVG Figure（科学/教材 SVG 作图）

把「示意图设计规范」与「可交付 SVG 技术」合一，画**说明性（非数据驱动）**科学图：通路、机制、分类、知识地图。

## 何时用
- 画**通路/机制/分类/知识地图**等示意图（生物/医学/教材）
- 要 **SVG 矢量**、可编辑（进 Illustrator）、可出版（NR 风）
- 需固定规范（尺寸/字号/配色/字体）批量统一产出

流程：单图快出 → §八（工作流）；需多轮审/多角色 → §十一（多 Agent）。

## 一、示意图类型
| 类型 | 用途 | 关键元素 | 例 |
|---|---|---|---|
| 通路图 | 网络关系 | 节点、边(激活/抑制)、方向 | 代谢通路、信号级联 |
| 机制图 | 分步分子事件 | 蛋白、膜、因果箭头 | 酶促、剪切、转录 |
| 实验流程 | 协议步骤 | 编号步、设备图标、时间箭头 | 测序流程 |
| 知识地图/分类 | 概念结构 | 中心节点+分支 | 分类树、章总结 |
| 结构图 | 分子构象 | 带状/表面结构 | 蛋白域、配体口袋 |

要「发生什么」→工作流；要「因果」→机制/通路图。

## 二、SVG 技术规范
1. 画布：正确 `viewBox`（如 `0 0 1500 1000`）+ 物理尺寸 `width="150mm" height="100mm"`，先定出版尺寸再排元素。
2. 文本：显式 `font-family/font-size`；中文黑体（`PingFang SC, Heiti SC`），英文 `Helvetica/Arial`；字号三档 7.5/6/5pt（fs 26/21/17 @1500×1000），最小不 <5pt。
3. 样式：用**标准属性**（fill/stroke 直接写元素上），避免内联 `style/class`（Illustrator 兼容差——踩过坑）。
4. 可移植：禁内嵌字体/乱码 emoji/多余滤镜/base64。
5. 校验：`svglint` + 打开/inspect（裁剪/缩放/对比/文字/路径）。
6. 交付：先交 SVG，再交验证 + 渲染器/字体说明。

## 三、设计/出版规范
- 颜色语义一致：DNA 蓝/灰、mRNA 橙黄、同一蛋白全篇一色、核浅灰蓝、膜米黄双层、线粒体橙、溶酶体红紫。
- 箭头语义一致且配图例：`→`直接、`- - →`间接、`⊣`抑制；**勿只靠颜色**（红绿色盲），用形状区分。
- ≤8 元素/面板；复杂放补充图。
- 文字 ≥7pt（教材按三档，短标注可 5pt、正文 ≥6pt，别 ≤4.5pt）。
- 版权：用授权库（BioRender/Reactome CC-BY/Bioicons），重绘勿截图。
- 结构图加比例尺（ChimeraX `scalebar`）。
- 出版合规：工具出版许可、BioRender attribution 进图注、CMYK、无数据驱动图用矢量。

## 四、图形语言与验收 Gate（防「文字化」）
> 症状：图做成「色块排文字 / 提纲页」，图形元件≈0。本 skill 最易漂移，按 G1–G7 执行。

🔴 **交付闸门**：G3 密度 / G6 字体 / G7 重叠 不达标 → 🛑 STOP 回 Planner 元件化，禁止直接交付；人眼终审前不得交付。

**G1 概念→元件映射（Planner 硬性）**：先指派图形元件再动笔，禁把段落分区当布局。
| 概念 | 元件 |
|---|---|
| 小分子/代谢物 | 分子球 `ball`（带高光）；甾体/多环用 `ring4`（甾核四环簇）|
| 酶/转运体 | 圆角酶块 `enzyme`；膜转运体=双线膜带+跨膜双蛋白块（ABCG5/G8）|
| 信号蛋白/修饰 | 蛋白条 `protein`；磷酸化=红点 `P`；激活=`→`、抑制=`⊣`(tee)|
| 细胞/核 | 细胞椭圆 `cell`+核 `nucleus`；膜蛋白画在细胞轮廓|
| 膜 | 磷脂双分子层带（21px 间距标准）或简化双线膜|
| 器官/管道/回路 | 器官图标+回路 `path` 弧线（如门静脉肠肝循环）|
| 物质流 | 箭头；多球流=若干分子球沿线（如 FFA 涌入）|

**G2 文本禁令**：面板/节点内**禁完整句**——框内 ≤8 汉字（缩写 ≤14 拉丁字符），每要素只留名词/短短语。整句只允许：①图注（Word）②图底部 ≤2 行。>12 字框内文本=返工信号。

**G3 密度指标（Critic 程序自查）**：图形元素数 ≥ **5×** text 条数（标杆 F7≈12:1）；text 均长 ≤8 字、单条最长 ≤28 字（仅图题/图底 ≤2 条）。
```python
# 图形:文本比、均长/最长
import re; raw=open('fN_pub.svg',encoding='utf-8').read()
shapes=len(re.findall(r'<(circle|ellipse|path|polygon|rect|line)\b',raw))
texts=re.findall(r'>([^<>]{2,})</text>',raw)
print(f'图形≈{shapes} text={len(texts)} 比≈{shapes/len(texts):.1f} 均长≈{sum(map(len,texts))/len(texts):.0f} 最长={max(map(len,texts))}')
```
（比值≥5、均长≤8；不达标→回 Planner，禁交付）

**G4 字号三档**：7.5pt(fs26)=图题/强调；6pt(fs21)=面板标题/酶蛋白名/主通路标签（图内主体默认）；5pt(fs17)=引线标注/图例名/分子旁缩写。**禁 <17px**；禁为凑字号放大长文本——改短标签才是出路。

**G5 无视觉 agent 兜底**：先跑 G3 统计拦截文字墙，再人眼终审。

**G6 字体按内容分派**：cairosvg/cairo 按 font-family 列表**取首个渲染整段、不逐字符回退**（`fc-match "Arial, Heiti SC,…"` 只返回 Arial；Arial 渲染「代谢」墨迹 466 vs Heiti SC 2016）→ 依赖多字体回退=中文整体丢失。写法：`txt()` 检测 `[\u4e00-\u9fff]`，含中文→`FONT_CN`（Heiti SC 优先，PingFang 兜底）；纯英文→`FONT_EN`（Arial）。验证：Arial 渲染中文墨迹<1000 即缺字。

**G7 空间不重叠（几何闸门）**：text–text 包围盒相交、text 出画布、text 落入非所属色块（enzyme/protein 自带标除外）、箭头端点贴节点边 0–3px。间距：面板内 ≥20px、文本行距 ≥字高+8；跨面板箭头走预留缝，不横穿面板/膜；箭头旁标签在线中点上方 ≥18px 或线外侧。密度观感：勿过散（用细胞/器官容器收拢）、勿过挤（20–60px 留白）。

## 五、NR 级默认要求（评审固化，新图一律）
1. 箭头语义统一 + 加图例。
2. 标签名词化 ≤8 字、≥7pt。
3. 每面板 ≤8 元素。
4. 生物实体一色一义；文字对比 ≥3:1；配色进图例。
5. 默认光影体积（radialGradient+高光+微投影）；描边 0.75pt。
6. 结构图加比例尺。
7. 矢量源导出 ≥300dpi；源文件版本管理（`fN_gen.py` 归档）。
8. 图标用授权库/自绘。

## 六、教材固定规范（bio 教材章）
| 项 | 固定值 |
|---|---|
| 图幅 | 通栏 **150×100mm** |
| 分辨率 | ≥300dpi（导出 1772×1181）|
| 字号 | 三档 7.5/6/5pt（fs 26/21/17）；禁 <17px；正文长句 ≥6pt |
| 字体 | 含中文→黑体（Heiti SC/STHeiti/PingFang）；纯英文→Arial；禁单一混合列表（G6）|
| 色板 | 蓝#4A7DBF 橙#E8A87C 紫#9B7FC0 绿#8FBF8F 粉#D98A8A 灰#5A5A5A 抑制#C0392B；背景白 |
| 膜密度 | **间距 21px（@1500 宽画布）**＝头 r7、尾 ±5，4 倍密度（F2 定稿）|
| 分子结构 | **RDKit**（SMILES→真实结构，官能团橙高亮）；进图转 **PDF** 供 AI 或独立结构 PNG |
| AI 修图 | 打开 PDF：字体统一 **Arial Bold**、描边统一 **0.75pt** |
| 图注 | 放 Word（图下**宋体 6pt**），不入图 |

## 七、既有图（AI 导出）NR 增强 + 进阶质感（复盘定稿）
对已 AI 导出的图，**只增强质感、不改排版**。工具：`scripts/nr_color_audit.py` + `scripts/nr_enhance.py`（GitHub `wangseeker/bio-svg-figure`）。
1. **配色审计+校正**：`nr_color_audit.py <f>.svg --prefix=<f>` → `_NR配色.svg`（莫兰迪降饱和、红绿检查、对比、色相精简）
2. **进阶质感**：`nr_enhance.py <f>_NR配色.svg --prefix=<f>` → `_NR进阶.svg`（极浅暖白底+通用光影+0.75pt+无标尺）
3. **字体归一**（AI 内嵌名→SimHei/Arial+bold）→ 导出 300dpi PNG

**定稿参数（勿改）**：极浅暖白底 `#FFFFFF→#FCFAF6→#F4F0E9`（绝不灰暗）；只降 **S>0.62** 高饱和、不压明度；**通用光影**（每非文字主填充色→`gx_*` 径向渐变，fill→`url(#gx_*)`）；描边 0.75pt；中文 SimHei/英文 Arial；无标尺默认（结构图才 `--scalebar`）。

**复盘 7 坑（详见 `references/NR增强进阶SOP.md`）**
1. 背景渐晕做深→灰蒙→极浅暖白
2. 全局降饱和+压明度→发灰→只降 S>0.62
3. 光影只映射解剖色→其余图光影引用=0→通用光影（自检 `grep -c url(#gx_` >0）
4. AI 内嵌字体名（Arial-BoldMT/ArialMT/STHeitiSC-Light/KozGoPr6N）→PNG乱码→归一 SimHei/Arial+bold（自检 grep 无输出）
5. 比例尺误加→默认关
6. 中间产物多→只留最新 `_NR进阶.svg`+`_出版300dpi.png`；原始另存 `原始/`
7. 图号/图注错位→按内容对齐重排 f1–f17；正文定稿版"图12-1~17"天然对应；Figure-Legends.docx 同步重排

## 八、工作流
1. 定主题/要素（该画什么、分几区、标签，忠于原文）
2. 定图幅/字号/色板（§六）
3. 元件化拼装（`scripts/elements.py`，示例见 `references/教材作图批量方法.md`）
4. 生成 SVG（标准属性、数值化 scale、物理 mm）→ 渲 PNG 校验
5. 交付：SVG（AI 开）+ PDF（AI 兼容）+ 300dpi PNG
6. 人眼验收（勿凭机械评审）→ 精修
7. 图注入 Word/论文

## 九、🛟 故障与兜底（触发→一线修复→仍失败兜底）
| # | 故障 | 一线修复 | 仍失败兜底 |
|---|---|---|---|
| 1 | svglint 报错 | 修 XML 转义、viewBox 匹配、根 xmlns | 渲染器直开目检，声明范围受限 |
| 2 | PNG 中文整体消失 | G6：中文走 FONT_CN，确认 fc-match 可用名 | 交付 SVG 并标注字体要求，不静默降级 |
| 3 | 文字墙/形文<5:1 | G1 元件映射 + G2 删句成标签 | 拆面板/缩内容，标 [DENSITY_PARTIAL] |
| 4 | 元素重叠/箭头悬空 | G7 几何自检（间距≥20、箭头贴边 0-3px）| 回 Planner 重排 |
| 5 | 字号<下限 | G4 缩内容/改短标签，禁放大 | 拆面板分图 |
| 6 | RDKit 嵌入问题 | 转 PDF 或独立 PNG | 标占位待补，不猜结构 |

## 十、踩过的坑
- ❌ AI 生图（错字/漂移/位图）→ 程序化 SVG
- ❌ 内联 style/class → Illustrator 不显示 → 标准属性
- ❌ SVG 直开 AI 兼容差 → PDF/独立结构图
- ❌ 机械评审判视觉 → 人眼+矢量放大
- ❌ 磷脂双分子层繁琐 → 简洁线条膜
- ❌ 颜色混乱 → 统一色板
- ❌ 先作图后定版式 → 先定尺寸/字号/色板
- ❌ SVG scale 除号 → 先算成数字
- ❌ 色块装整句（提纲化/文字页）→ G1+G2（F12/F13 返工）
- ❌ 无视觉 agent 直交文字墙 → G3 自查（≥5:1、均长≤8）
- ❌ 为凑字号放大长文本 → 短标签+字号分级 G4

## 十一、多 Agent 流水线（PaperBanana → 程序化 SVG）
| Agent | SVG 版 | 做什么 | 产出 |
|---|---|---|---|
| Retriever | 要素/术语检索 | 从原文提取要素/术语（忠于原文勿编造）| 要素清单 |
| Planner | 布局规划 | 定分区/节点序/箭头/标签 | 布局方案 |
| Stylist | 风格规范 | 套固定常量（150×100mm、三档字号、NR 色板、标准属性）| 规范参数 |
| Visualizer | SVG 生成器 | 元件函数拼装（**程序画**，非 AI 生图）| SVG |
| Critic | 校验器 | XML 合法+渲染 PNG+对齐/尺寸/字号/色板+G3 密度统计；**人眼终审** | 校验报告 |

执行：①Retriever→Planner 出方案 → 🔴 CHECKPOINT 用户审（确认后才生成 SVG）②Stylist→Visualizer 生成 ③Critic 程序校验+渲 300dpi ④人眼终审通过才交付，否则回 Planner/Visualizer。

**为何不用 AI 生图**：Visualizer 程序化（零漂移/矢量/可编辑/满足投稿）；Critic 程序校验+人眼（非 AI 评审）；Retriever 用原文（忠于内容）。

## 十二、Related
- `scientific-visualization` — 数据图
- `scientific-manuscript-writing` — 图注与论文整合
- `textbook-chapter-review` — 教材术语/缩写
