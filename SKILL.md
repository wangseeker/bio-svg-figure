---
name: bio-svg-figure
description: 设计与生成高质量科学/教材示意图（SVG 矢量，Nature Reviews 风）。融合 creating-svg-illustrations 的 SVG 技术规范（画布/样式/文本/验证/嵌入）与 scientific-schematics 的设计出版规范（示意图类型/配色语义/箭头规范/≥7pt/色盲/出版维度），并内置生物教材固定规范（150×100mm、7.5/6/5pt、NR 色板、RDKit 真实分子结构、PNG 落位）。用于绘制通路图/机制图/分类图/知识地图等教学与论文配图。Trigger on：画通路/机制/分类/知识地图等示意 SVG 时使用。
license: "CC-BY-4.0"
---

# Bio SVG Figure（科学/教材 SVG 作图）

把"科研示意图（scientific schematics）"的设计原则与"可交付 SVG"的技术实现合二为一，用于绘制通路图、机制图、分类图、知识地图等**说明性（非数据驱动）**科学图。

## 何时用
- 需要画**通路/机制/分类/知识地图**等示意图（生物、医学、教材）
- 需要**SVG 矢量**、可编辑（进 Illustrator）、可出版（NR 风）
- 需要固定规范（尺寸/字号/配色/字体）统一成批产出

**流程选择**：单图快速/直接交付 → 走「六、工作流」（默认）；需多轮审图或多 agent 分角色 → 走「九、多 Agent 流水线」。

## 一、示意图类型与选择（来自 scientific-schematics）
| 类型 | 用途 | 关键元素 | 例子 |
|------|------|---------|------|
| 通路图 Pathway | 网络关系 | 节点(蛋白/代谢物)、边(激活/抑制)、方向 | 代谢通路、信号级联 |
| 机制图 Mechanism | 分步分子事件 | 蛋白、膜、箭头表示因果 | 酶促、剪切、转录 |
| 实验流程 Workflow | 协议步骤 | 编号步、设备/样本图标、时间箭头 | 测序流程 |
| 知识地图/分类 | 概念结构 | 中心节点+分支 | 分类树、章总结 |
| 结构图 Structural | 分子构象 | 带状/表面结构 | 蛋白域、配体口袋 |

**决策**：要"发生什么"→工作流；要"为什么（因果）"→机制/通路图；30 秒论文摘要→graphical abstract。

## 二、SVG 技术规范（来自 creating-svg-illustrations）
1. **画布**：正确 `viewBox`（如 `0 0 1500 1000`）、物理尺寸（`width="150mm" height="100mm"`），先定出版尺寸再排元素。
2. **文本**：显式 `font-family/font-size`；中文优先字体（`PingFang SC, Heiti SC`），英文 `Helvetica/Arial`；字号按**三档分级 7.5/6/5pt**（fs 26/21/17，@1500×1000），图内最小不 <5pt。
3. **样式**：用**标准属性**（fill/stroke/stroke-width 直接写在元素上），避免内联 `style`/`class`（Illustrator 兼容差——**这正踩过坑**）。
4. **可移植**：避免内嵌字体/乱码 emoji/不必要的滤镜/base64（除非必须自包含）。
5. **验证**：`svglint` 校验 + 打开/inspect 裁剪、缩放、对比、文字、相对路径。
6. **交付**：先交 SVG 工件，再交做的验证 + 任何渲染器/字体说明。

## 三、设计/出版规范（来自 scientific-schematics）
- **颜色语义要一致**：DNA 蓝/灰、mRNA 橙黄、蛋白=同一家族全篇一色、细胞核浅灰蓝、**膜=米黄双层**、线粒体橙、溶酶体红紫。
- **箭头语义一律且配文字图例**：`→`=直接激活、`- - →`=间接、`⊣`(平头)=抑制；**勿只靠颜色**（红绿 → 色盲），用形状区分。
- **≤8 元素/面板**：通路别超载，复杂上下文放补充图。
- **文字 ≥7pt**（按出版尺寸）；教材示意图按三档分级，**短标注可至 5pt**、正文级 ≥6pt，别 ≤4.5pt。
- **版权**：图标用授权库（BioRender 出版许可/Reactome CC-BY/Bioicons）；**重绘勿截图**。
- **结构图加比例尺**（ChimeraX `scalebar` 等）。
- **出版合规**：工具出版许可、BioRender attribution 进图注、CMYK（Inkscape 走 Ghostscript）、无数据驱动的图用矢量。

## 四、图形语言与验收 Gate（防“文字化”）
> 症状：图做得像“色块排文字 / 提纲页”——大段句子装进矩形，图形元件≈0。用户判“没有图，只有文字，全都错了”。这是本 skill 最易犯的漂移，务必按 G1–G5 执行。

🔴 **交付闸门（每次作图）**：G3 密度 / G6 字体 / G7 重叠 三项自查不达标 → 🛑 STOP 回 Planner 元件化，禁止直接交付；人眼终审前不得交付。

**G1 概念→元件映射（Planner 硬性步骤）**：每个科学概念必须先指派一种**图形元件**再动笔，禁止“把段落文字分区”当布局：

| 概念 | 元件 |
|---|---|
| 小分子/代谢物 | 分子球 `ball`（带高光）；甾体/多环分子用 `ring4`（甾核四环簇：胆固醇/胆汁酸/粪固醇） |
| 酶/转运体 | 圆角酶块 `enzyme`；膜转运体 = 双线膜带 + 跨膜双蛋白块（如 ABCG5/G8） |
| 信号蛋白/修饰 | 蛋白条 `protein`；磷酸化 = 红点 `P`；激活 = 箭头、抑制 = `⊣`(tee) |
| 细胞/核 | 细胞椭圆 `cell` + 核 `nucleus`；膜蛋白（GLUT4 等）画在细胞轮廓处 |
| 膜 | 磷脂双分子层带（21px 间距标准）或简化双线膜 |
| 器官/管道/回路 | 器官图标（肝/胆囊椭圆/肠管圆角管）+ 回路用 `path` 弧线（如门静脉肠肝循环） |
| 物质流 | 箭头；多球流 = 若干分子球沿线（如 FFA 涌入） |

**G2 文本禁令**：面板/节点内**禁止完整句**——框内标签 ≤8 汉字（分子旁缩写 ≤14 拉丁字符），每要素只留名词/极短短语（“IRS-1 丝氨酸磷酸化”画成 蛋白条 IRS-1 + P 点，不写整句）。整句说明只允许：① 图注（放 Word）；② 图底部 ≤2 行。出现 1 处 >12 字的框内文本 = 返工信号。

**G3 图形密度指标（Critic 程序自查，未达标不交付）**：
- 图形元素（circle+ellipse+path+polygon）数 ≥ **5×** text 条数（标杆：F7≈12:1，F13v4/F12v3≈5–8:1）
- text **平均长 ≤8 字**；单条最长 ≤28 字且仅限图题/图底说明（≤2 条）
```python
# 自查：图形:文本比、text 均长/最长
import re; raw=open('fN_pub.svg',encoding='utf-8').read()
shapes=len(re.findall(r'<(circle|ellipse|path|polygon|rect|line)',raw))
texts=re.findall(r'>([^<>]{2,})</text>',raw)
print(f'图形≈{shapes} text={len(texts)} 比值≈{shapes/len(texts):.1f}:1 均长≈{sum(map(len,texts))/len(texts):.0f} 最长={max(map(len,texts))}')
```
（预期 比值≥5、均长≤8；不达标 → 回 Planner 元件化，禁止直接交付）

**G4 字号三档分级（7.5/6/5pt @1500×1000 → fs 26/21/17）**：①7.5pt(fs26)=图题/阶段大字/最强调；②6pt(fs21)=面板标题、酶·蛋白名、主通路标签（**图内主体默认档**）；③5pt(fs17)=引线标注、图例名、分子旁缩写等**最短**内容。**纪律**：图内禁止 <17px（<4.8pt）；正文性/连续 >2-3 行内容勿用 5pt；**禁止为凑字号下限放大长文本**——把句子改短才是出路，文字放大是文字化的帮凶。

**G5 无视觉 agent 兜底**：生成后先跑 G3 统计自查拦截“文字墙”，再交人眼终审。教训：缺此闸门时 agent 会交付整页文字图，全靠用户返工。

**元件补充**：`ring4` 甾核环簇（胆固醇橙/胆汁酸绿/粪固醇棕）；`pdot` P 磷酸化红点；`tee` ⊣ 抑制头；`cell`+`nucleus`；膜转运体双块；器官图标（胆囊椭圆、肠管分段、回路 path 弧）；脂滴高光球。范例与指标见 `references/pattern-examples.md`。

**G6 字体按内容分派**
- **渲染器事实**：`cairosvg`/cairo 按 font-family 列表**取第一个可用字体渲染整段文本，不做逐字符回退**（`fc-match "Arial, Heiti SC, ..."` 只返回 Arial；像素级测试：Arial 渲染「代谢」≈466 墨迹 vs Heiti SC 2016）。依赖"列表多字体回退"= 中文整体丢失。
- **写法**：`txt()` 检测 `re.search(r"[\u4e00-\u9fff]", s)`——含中文 → `FONT_CN`（黑体 Heiti SC 优先、PingFang 兜底）；纯英文/数字/希腊 → `FONT_EN`（Arial）。同一标签中英混排（如"门静脉FFA"）整体走中文族；要严格汉字黑体+英文 Arial 需拆元素，一般不做。
- **验证**：渲染后抽查单字体 SVG——Arial 渲染「代谢」墨迹<1000 即缺中文（黑体/苹方≈2000+）；`fc-match "Heiti SC"` 确认系统可用名。

**G7 空间不重叠（几何自检闸门）**
- **无视觉 agent 必跑程序化重叠检测**（0 重叠才交付人眼）：
  1. text–text 包围盒相交（宽估：中文/全角=fs、ASCII≈0.58fs、空格≈0.32fs；按 anchor 定左缘）
  2. text 出画布（0–1500×0–1000）
  3. text 中心落入"非所属"色块（enzyme/protein 自带标签除外）
  4. 箭头端点**必须贴节点边**（块边/椭圆边，头尖贴边 0–3px；禁止悬空≥8px 或深扎）
- **布局间距**：面板内元素 ≥20px、文本行距 ≥字高+8；跨面板箭头走**预留缝隙**（如 520–660 胞外带），不横穿面板内容/细胞膜；箭头旁标签放线中点**上方 ≥18px** 或线外侧，不与线相交。
- **密度观感（用户两次校准）**：勿过散——避免"全宽横带平铺/大空洞"，用细胞/器官等**容器**收纳元件更聚拢；勿过挤——元素间 20–60px 均匀留白为佳。

## 五、教材固定规范（我们沉淀，bio 教材章用）
| 项 | 固定值 |
|----|--------|
| 图幅 | 通栏 **150×100mm**（3:2）＝150mm 版心宽 |
| 分辨率 | ≥300dpi（SVG 矢量；导出 1772×1181 PNG）|
| 字号 | **三档分级（7.5/6/5pt，@1500×1000 对应 fs 26/21/17）**：7.5pt=图题/阶段大字/关键强调；6pt=面板标题/酶·蛋白名/主通路标签（图内主体默认）；5pt=引线标注/图例名/分子旁缩写等**最短内容**。**禁止 <17px**（图注级长句 ≥6pt）|
| 字体 | **按内容分派**：含中文 → 黑体（`FONT_CN = 'Heiti SC', STHeiti, 'PingFang SC'`）；纯英文/数字/希腊 → **Arial**（`FONT_EN = Arial, Helvetica`）。禁止用单一混合列表（见 G6）|
| 色板 | 蓝#4A7DBF 橙#E8A87C 紫#9B7FC0 绿#8FBF8F 粉#D98A8A 灰#5A5A5A 抑制#C0392B；背景白/分区淡色 |
| **磷脂双分子层密度** | **间距 21px（@1500px 宽画布）**＝头 r7、尾偏移 ±5，4 倍密度标准（F2 定稿，以后所有膜图用此密度）|
| 分子结构 | **RDKit**（SMILES→真实结构，官能团橙高亮），矢量可编辑；进图尽量**转 PDF** 供 Illustrator（SVG 直接开有兼容坑）或**独立结构 PNG** 由用户手工摆放 |
| AI 修图（Illustrator） | 打开 PDF 修图：字体统一 **Arial Bold**、所有描边统一 **0.75pt**；其余样式不变 |
| 图注 | 放 Word（图下方**宋体 6pt**），不入图 |

## 六、工作流
1. 定主题/要素（该画什么、分几区、标签文字，忠于教材/论文）
2. 定图幅、字号、色板（第二、五节常量）
3. 元件化拼装（ball/node/arrow/beta_cycle/membrane/region 等函数；函数库与拼装示例见 ~/.reasonix/vault/Writing/textbook/figures/教材作图批量方法.md）
4. 生成 SVG（**标准属性**，勿 style/class）→ 渲染 PNG 校验
5. 交付：SVG（AI 开）＋ PDF（AI 兼容好）＋ 300dpi PNG（出版）
6. 人眼验收（勿凭机械评审）→ 验收后按需精修
7. 图注入 Word/论文

## 七、🛟 故障与兜底（三段式：触发条件 → 一线修复 → 仍失败兜底）

| # | 故障 | 一线修复 | 仍失败兜底 |
|---|------|----------|-----------|
| 1 | svglint 校验报错 | 按报错修：XML 转义（&amp;/&lt;/&gt;）、viewBox 匹配内容、根元素 xmlns | 仍失败：浏览器/渲染器直开目检，声明验证范围受限，不交付未验证 SVG |
| 2 | PNG 中文整体消失 | 按 G6：含 [\u4e00-\u9fff] 的文本走 FONT_CN（Heiti SC 优先），确认 fc-match 系统可用名 | 仍失败：交付 SVG 并标注目标环境字体要求，不静默降级 |
| 3 | 文字墙 / 图形:文本 <5:1 | 回 G1 元件映射 + G2 删句成标签（框内 ≤8 字） | 仍失败：拆面板/缩内容，标注 [DENSITY_PARTIAL]，交人眼终审判 |
| 4 | 元素重叠 / 箭头悬空 | 跑 G7 几何自检逐项修（间距 ≥20px、箭头贴节点边 0-3px） | 仍失败：回 Planner 重排布局 |
| 5 | 字号低于下限（<17px） | 按 G4 缩内容/改短标签，禁止放大文字凑下限 | 内容仍超：拆面板分图 |
| 6 | RDKit 分子结构嵌入问题 | 结构转 PDF（AI 兼容）或独立 PNG 由用户摆放 | 标注占位待补，不猜测结构 |

---

## 八、踩过的坑（教训，直接照做）
- ❌ AI 生图（错字/漂移/位图）→ 程序化 SVG
- ❌ SVG 内联 style/class → Illustrator 不显示 → **标准属性**
- ❌ SVG 直接开 AI 兼容差 → 用 **PDF** 或独立结构图
- ❌ 机械评审（VL）判视觉 → 以人眼+矢量放大
- ❌ 磷脂双分子层繁琐 → 简洁线条膜
- ❌ 颜色混乱 → 统一色板
- ❌ 先作图后定版式 → **先定尺寸/字号/色板**
- ❌ SVG `scale(334/360)` 除号 → 先算成数字
- ❌ 色块装整句（提纲化/文字页，图形≈0）→ 概念→元件映射 G1 + 文本禁令 G2（F12/F13 返工：用户“没有图只有文字”）
- ❌ 无视觉 agent 直交文字墙 → 跑 G3 统计自查（图形:文本≥5:1、均长≤8）
- ❌ 为字号下限放大长文本 → 短标签 + 字号分级 G4（放大文字=文字化帮凶）

## 九、多 Agent 流水线（借鉴 PaperBanana，适配程序化 SVG）
> PaperBanana 用 Retriever/Planner/Stylist/Visualizer/Critic 五智能体做"AI 生图"；我们把它**映射到程序化 SVG**（Visualizer=程序生成而非图像生成，Critic=程序校验+人眼，规避 AI 漂移/错字/位图）。

| PaperBanana Agent | 我们的 SVG 版 | 做什么 | 产出 |
|-------------------|--------------|--------|------|
| **Retriever** | 要素/术语检索 | 从教材/论文原文提取该图要素、术语（忠于原文，勿编造）| 要素清单 |
| **Planner** | 布局规划 | 定分区、节点序、箭头关系、标签文字（先出"草图规划"）| 布局方案 |
| **Stylist** | 风格规范 | 应用固定常量：150×100mm、9/7.5/6pt、NR 色板、中文字体、标准属性 | 规范参数 |
| **Visualizer** | SVG 生成器 | 元件函数拼装生成 SVG（程序画，**非 AI 生图**）| SVG |
| **Critic** | 校验器 | XML 合法 + 渲染 PNG + 对齐/尺寸/字号/色板 + **G3 图形密度统计（图形:文本≥5:1、均长≤8）**；**人眼终审**（勿凭机械评审）| 校验报告 |

### 流水线执行
1. **Retriever** 出要素清单 → **Planner** 出布局方案 → 🔴 CHECKPOINT：两者先给用户审（确认后才生成 SVG，避免画错）
2. **Stylist** 套规范参数 → **Visualizer** 生成 SVG（标准属性、数值化 scale、物理 mm）
3. **Critic**：程序校验（XML 合法、viewBox/尺寸、字号、色板、对齐）+ 渲 300dpi PNG
4. **人眼终审**（用户开 SVG/PNG 放大看）→ 通过则交付；不通过回 Planner/Visualizer 迭代

### 关键差异（我们为什么不"AI 生图"）
- Visualizer 用**程序化 SVG**（零漂移、矢量、可编辑、满足投稿）而非图像生成（错字/漂移/位图）
- Critic 用**程序校验 + 人眼** 而非 AI 评审（机械评审判视觉不可靠）
- Retriever 用**教材/论文原文**（忠于内容），不是任意参考图

## 十、Related
- `scientific-visualization` — 数据图（图/热图）
- `scientific-manuscript-writing` — 图注与论文整合
- `textbook-chapter-review` — 教材术语/缩写规范
